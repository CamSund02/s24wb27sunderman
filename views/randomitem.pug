extends layout

block content
  h1= title
  - var image_names = ["camera1.jpg", "camera2.jpg", "drone1.jpg", "drone2.jpg", "skateboard.jpg"]

  - // Loop three times
  - for (let i = 0; i < 3; i++)
    - let randomIndex = Math.floor(Math.random() * 5)
    - let imageName = image_names[randomIndex]
    img(src=`/images/${imageName}`, alt=imageName, height="200", width="200")
